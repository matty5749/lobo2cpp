Bonjour.

Avant toute chose , il vous faut les droits d'execution des scripts présents dans ce dossier:

Placez vous dans le repertoire lobo2cpp avec un terminale et tapez la commande:
chmod +x *.sh

Je suppose que vous avez installer le logiciel cmake , si ce n'est pas le cas:
sudo apt-get install cmake
mdp:

L'installation de lobo2cpp se fait par l'execution du script install.sh :
./install.sh


